version: v2
managed:
  enabled: true
plugins:
    # The bundled buf.validate stub is relocatable protobuf-py gencode only
    # (`*_pb.py`); it ships inside protovalidate so users need not add the
    # protos themselves.
    - local: protoc-gen-py
      out: protovalidate/_gen
